# Vite reads env from this directory — only VITE_* keys are exposed to the browser.
# Use an explicit API origin for local dev (no Vite proxy in the current scaffold).
# Value is the API base only — no path suffix; the client calls GET {VITE_API_BASE_URL}/todos.

VITE_API_BASE_URL=http://localhost:3000
